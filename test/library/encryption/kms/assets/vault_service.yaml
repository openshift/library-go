apiVersion: v1
kind: Service
metadata:
  name: vault
  namespace: {{ .Namespace }}
  labels:
    app: vault
spec:
  ports:
    - name: http
      port: 8200
      targetPort: 8200
  selector:
    app: vault
  clusterIP: None
